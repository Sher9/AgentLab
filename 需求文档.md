# AI Agent 开发学习网站 · 需求文档（PRD）

> 版本：v0.2（需求确认版）
> 日期：2026-07-29
> 目标：先定需求，再进入开发。本文档是后续设计与编码的基准。

---

## 1. 项目定位

一个**面向有开发经验者**的 AI Agent 开发学习网站。内容以「教程 + 课程大纲 + 可运行示例」为主，形态上是**静态教程站**（构建期生成静态页面），技术上用 **Next.js 全栈框架**实现，最终交付为**可直接上线**的成品（SEO / 响应式 / 性能达标）。

核心理念：**边学边做**——网站本身用真实 Agent 项目最常用的技术栈搭建，读者在学 Agent 开发的同时，也间接学到了现代全栈工程实践。

---

## 2. 目标用户

| 维度 | 说明 |
|------|------|
| 身份 | 已有编程经验（前端/后端/算法均可），想系统上手 AI Agent 开发 |
| 痛点 | 框架文档碎片化、概念多但不成体系、缺"从 0 到 1 跑通"的实战 |
| 期望 | 重实战、轻口水概念；能复制代码直接跑；有清晰的学习路径 |
| 不服务 | 纯零基础小白（本版不单独铺垫编程基础） |

---

## 3. 核心功能（必备 MVP）

1. **落地页（首页）**：一句话说清站点价值 + 学习路径概览 + 热门/入门入口。
2. **课程大纲页**：模块化课程结构（章节树），显示进度与难度。
3. **文章详情页**：教程正文，支持 MDX（代码块 + 提示框 + 可运行示例占位）。
4. **导航与搜索**：顶部导航 + 站内搜索（静态站用客户端/构建期索引）。
5. **关于 / 资源页**：参考资料、外部链接、更新日志。

### 进阶（上线成品阶段补齐）
- 暗色模式切换
- 代码语法高亮 + 一键复制
- 阅读进度 / 上一篇下一篇
- SEO 全套（meta、OG 图、sitemap、RSS）
- 响应式适配（移动端可读）

---

## 4. 信息架构（IA）

```
/                 首页（落地页）
/courses          课程大纲（模块 + 章节树）
/courses/[slug]   单篇教程（MDX 渲染）
/about            关于 & 资源
/search           搜索结果（或全局浮层）
```

内容以 **MDX 文件** 存放在仓库 `content/` 目录，构建期编译为静态页面（SSG）。

---

## 5. 技术方案

| 层 | 选型 | 理由 |
|----|------|------|
| 框架 | Next.js (App Router) + TypeScript | 真实 Agent 项目主流；SSG 满足"静态站"诉求 |
| 样式 | CSS Modules + 原生 CSS（**不使用 Tailwind**） | 设计令牌驱动、组件级作用域，零运行时依赖，易维护 |
| 内容 | MDX (`content/*.mdx`) + 构建期索引 | 教程即代码，版本可控，适合静态生成 |
| 高亮 | Shiki | 构建期高亮，零运行时成本，效果好 |
| 搜索 | Pagefind（构建期生成索引） | 为静态站而生，零后端 |
| 部署 | Vercel 或静态导出到任意主机 | 满足"可上线成品" |
| 工程化 | ESLint + Prettier + 目录约定 | 让读者学到规范做法 |

> 注：虽是"静态教程站"，但用全栈框架实现，后续可平滑升级为带账号/社区（如未来需要）。

---

## 6. 内容规划（AI Agent 开发课程体系）

面向有经验开发者，建议 8 个模块：

- **M0 起点**：什么是 Agent / LLM 能力回顾 / 开发环境搭建
- **M1 工具调用**：Function Calling / Tool Use / 参数校验
- **M2 架构模式**：ReAct、Plan-and-Execute、Reflexion
- **M3 主流框架**：原生 SDK、LangChain/LangGraph、LlamaIndex、AutoGen、CrewAI 对比
- **M4 记忆与状态**：短期/长期记忆、RAG 入门
- **M5 多 Agent 协作**：角色编排、通信、任务分解
- **M6 评估与可观测**：Tracing、Eval、Guardrails
- **M7 工程化与部署**：成本/延迟优化、流式、缓存
- **M8 实战**：从零搭一个可运行的 Agent（示例项目）

MVP 先写 **M0 + M1 各 1 篇示例（采用"能跑的骨架 + 详细注释"写法，且资料要更全）**，验证模板与排版后再批量填充。

---

## 7. 非功能需求（上线成品标准）

- **性能**：Lighthouse 性能/可访问性 ≥ 90
- **SEO**：每页独立 title/description、OG 图、sitemap.xml、robots.txt
- **响应式**：桌面 / 平板 / 手机均可用
- **可访问性**：语义化标签、键盘可达、对比度达标
- **可维护性**：内容/代码分离，新增文章只需加一个 MDX 文件

---

## 8. 里程碑（开发计划）

| 阶段 | 内容 | 交付物 |
|------|------|--------|
| M1 脚手架 | Next.js+TS+Tailwind 初始化、设计系统、布局 | 可运行的空站点 |
| M2 落地页 | 首页（价值主张 + 学习路径 + 入口） | 可视首页 |
| M3 内容骨架 | 课程大纲页 + 文章模板 + MDX 渲染 + 1 篇示例 | 可浏览教程 |
| M4 填充与搜索 | 多篇文章 + 搜索 + 导航/上一篇下一篇 | 接近成品 |
| M5 上线打磨 | 暗色模式、SEO、响应式、部署 | 可上线成品 |

---

## 9. 已确认决策（v0.2）

1. **站点名称**：**AgentLab**（已定）。
2. **前端样式**：**不使用 Tailwind**，改用 CSS Modules + 原生 CSS（设计令牌驱动、组件级作用域）。
3. **示例文章写法**：先写成"**能跑的骨架 + 详细注释**"——结构完整、可直接运行，关键处加注释讲清为什么。
4. **内容资料**：要求**更全**——概念、原理、对比、可复制代码、踩坑提示都要写足，不追求极简。

## 10. 待确认 / 开放项

1. **品牌色 / 风格**：默认科技蓝 + 暗色优先，可调整（未定）。
2. **是否现在就搭 MVP**：需求已确认，准备进入 M1 脚手架。

---

_下一步：你确认需求与站点名后，我直接进入 M1 脚手架搭建。_
