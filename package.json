{
  "name": "agentlab",
  "version": "0.1.0",
  "private": true,
  "description": "AgentLab 静态教程站 - 纯 HTML 生成器（无 Next.js）",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "dev": "node scripts/build-static.mjs && python -m http.server 8080 --directory static"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^18.0.7"
  }
}
